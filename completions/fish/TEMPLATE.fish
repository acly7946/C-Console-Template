# TEMPLATE(1) completion

complete -f -c TEMPLATE
complete -c TEMPLATE -s h -l help --description "Show help message and quit"
complete -c TEMPLATE -s V -l version --description "Show the version number and quit"